# Cloud Build config: build + push + deploy the FDP MCP server.
#
# See mcp_servers/sap/cloudbuild.yaml for full documentation.
#
# Submit from repo root:
#   gcloud builds submit --config mcp_servers/fdp/cloudbuild.yaml

substitutions:
  _REGION: us-central1
  _REPO: mcp-servers
  _IMAGE: fdp-mcp-server
  _SERVICE: fdp-mcp-server
  _SERVICE_ACCOUNT: fdp-mcp-sa

steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: bash
    args:
      - '-c'
      - |
        set -euo pipefail
        cp -R data mcp_servers/fdp/data
        ls mcp_servers/fdp/data | head -5
        # Stage agents/ package — see mcp_servers/sap/cloudbuild.yaml.
        mkdir -p mcp_servers/fdp/agents
        cp agents/schemas.py mcp_servers/fdp/agents/schemas.py
        touch mcp_servers/fdp/agents/__init__.py
        # Stage shared entrypoint.
        cp mcp_servers/entrypoint.sh mcp_servers/fdp/entrypoint.sh
        chmod +x mcp_servers/fdp/entrypoint.sh

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_IMAGE}:latest'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_IMAGE}:$BUILD_ID'
      - '-f'
      - 'mcp_servers/fdp/Dockerfile'
      - 'mcp_servers/fdp'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '--all-tags'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_IMAGE}'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - '${_SERVICE}'
      - '--image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_IMAGE}:$BUILD_ID'
      - '--region=${_REGION}'
      - '--platform=managed'
      - '--no-allow-unauthenticated'
      - '--service-account=${_SERVICE_ACCOUNT}@${PROJECT_ID}.iam.gserviceaccount.com'
      - '--port=8080'
      - '--cpu=1'
      - '--memory=512Mi'
      - '--min-instances=0'
      - '--max-instances=10'
      - '--quiet'

images:
  - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_IMAGE}:latest'
  - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_IMAGE}:$BUILD_ID'

options:
  logging: CLOUD_LOGGING_ONLY
